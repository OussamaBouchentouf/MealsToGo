import { colors } from "./colors";
import { space, lineHeights } from "./spacing";
import { sizes, sizesPourcentages } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";
import { phoneDimensions } from "../../utils/dimensions";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  sizesPourcentages,
  fonts,
  fontSizes,
  fontWeights,
  phoneDimensions,
};
